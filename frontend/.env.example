# Point this to your deployed backend API base URL (no trailing slash)
# Local dev:
VITE_API_URL=http://localhost:5000/api

# Production (set this in Vercel's Environment Variables, not in this file):
# VITE_API_URL=https://your-backend-name.onrender.com/api
